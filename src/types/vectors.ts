/** A 2D coordinate, used throughout the desktop UI for window/icon placement and dragging. */
export type Position = { x: number; y: number };
/** Width/height dimensions in pixels, used for sizing windows and other resizable UI elements. */
export type Size = { width: number; height: number };